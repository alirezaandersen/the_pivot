<h1>Active Companies </h1>
<h3>Total Active Companies <%= @companies.count %></h3>
<ul>
<% @companies.each do |company| %>
  <li><%= company.name %></li>
  <li><%= company.logo %></li>
  <li>Company Active Since<%= company.created_at %></li>

  <%= button_to "CREATE JOB", create_company_jobs_path(company.id),method: :get %>
  <%= button_to "INACTIVE COMPANY", inactivate_company_path(company.name),method: :get %>
  <%= button_to "*******CREATE ADDITIONAL ADMIN*******", inactivate_company_path(company.name),method: :get %>
  <%= button_to "*******CREATE ADDITIONAL ADMIN*******", inactivate_company_path(company.name),method: :get %>
  <%= button_to "*******CREATE ADDITIONAL ADMIN*******", inactivate_company_path(company.name),method: :get %>
  <%= button_to "*******CREATE ADDITIONAL ADMIN*******", inactivate_company_path(company.name),method: :get %>

<% end  %>
<ul>
