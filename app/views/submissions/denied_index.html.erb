<div class="container">
  <h1 class="center-align" id="company-title">Denied Submissions</h1>
  <div class="row">
    <% @submission.each do |company| %>
      <div class="col s12 m4">
        <div class="card">
          <div class="card-image">
            <img src="<%= company.logo %>">
          </div>
          <%= company.created_at %>
          <div class="card-action">
            <span class="card-title"> <%= link_to company.company_name, company_submission_status_path(company.company_name) %> </span>
          </div>
          <div class="card-content">
              <%= truncate(company.description, length: 57) %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
