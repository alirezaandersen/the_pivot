<div class="checkout-button">
  <% if current_volunteer %>
    <%= button_to "Checkout", commitments_path, class:"slider-btn waves-effect waves-light btn"%>
  <% else %>
    <%= button_to "Login or Create Account to Checkout", login_path, method: :get, class:"slider-btn waves-effect waves-light btn" %>
  <% end %>
</div>

<%= render partial: "shared/job_cards", locals: {tasks: @tasks, method: :delete } %>
